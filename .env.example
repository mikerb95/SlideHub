# ──────────────────────────────────────────────────────────────────
# SlideHub — Variables de entorno
#
# Copia este archivo como .env y rellena los valores reales.
# NUNCA hagas commit de .env (ya está en .gitignore).
# ──────────────────────────────────────────────────────────────────

# ═════════════════════════════════════════════════════════════════
# PRODUCCIÓN — AIVEN + MongoDB Atlas + Render
# ═════════════════════════════════════════════════════════════════

# GATEWAY — URLs de los servicios backend
CORE_SERVICE_URL=https://slidehub-core.onrender.com
AI_SERVICE_URL=https://slidehub-ai.onrender.com

# CORE SERVICE — PostgreSQL en Aiven
# https://aiven.io — Datos de conexión:
DB_HOST=slidelat-bd-slidelat.i.aivencloud.com
DB_PORT=21552
DB_NAME=defaultdb
DB_USERNAME=avnadmin
DB_PASSWORD=your_aiven_password_here
DB_SSL_MODE=require
DB_POOL_SIZE=3

# Redis en Render
# Render proporciona estas credenciales al crear el servicio Redis
REDIS_HOST=your-render-redis-host.onrender.com
REDIS_PORT=6379
REDIS_PASSWORD=your_render_redis_password
REDIS_SSL=true

# AI SERVICE — MongoDB Atlas
# https://www.mongodb.com/cloud/atlas — Free tier M0:
# Formato: mongodb+srv://usuario:contraseña@cluster.mongodb.net/slidehub_ai?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your_atlas_user:your_atlas_password@cluster.mongodb.net/slidehub_ai?retryWrites=true&w=majority

# ═════════════════════════════════════════════════════════════════
# IA SERVICES — APIs externas
# ═════════════════════════════════════════════════════════════════

# Groq AI — Generación de contenido y notas
# https://console.groq.com/keys
GROQ_API_KEY=sk_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GROQ_MODEL=llama-3.3-70b-versatile
GROQ_MAX_TOKENS=1024

# Gemini AI — Análisis de repositorios GitHub
# https://ai.google.dev/api/rest/generative-models-api
GEMINI_API_KEY=AIzaSy_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GEMINI_MODEL=gemini-2.0-flash
GEMINI_MAX_TOKENS=2048

# GitHub — Para análisis de repos (URL pública) o privados (OAuth/Token)
# OAuth App: https://github.com/settings/developers
GITHUB_CLIENT_ID=Ov23lixxxxxxxxxxxxxxxx
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret_here
# O usa un Personal Access Token para repos privados
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ═════════════════════════════════════════════════════════════════
# JVM
# ═════════════════════════════════════════════════════════════════
JAVA_OPTS=-XX:MaxRAMPercentage=75.0

# ═════════════════════════════════════════════════════════════════
# DESARROLLO LOCAL (docker-compose) — Valores por defecto funcionan
# ═════════════════════════════════════════════════════════════════
#
# Para development local, docker-compose.yml tiene valores por defecto:
# - PostgreSQL local en localhost:5432
# - MongoDB local en localhost:27017
# - Redis local en localhost:6379
# - Eureka deshabilitado
# Solo necesitas definir las claves de API externas si quieres probar IA:
# GROQ_API_KEY (opcional para probar Groq)
# GEMINI_API_KEY (opcional para probar Gemini)
#
#
