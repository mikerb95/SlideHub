# ──────────────────────────────────────────────────────────────────
# SlideHub — Variables de entorno
#
# Copia este archivo como .env y rellena los valores reales.
# NUNCA hagas commit de .env (ya está en .gitignore).
# ──────────────────────────────────────────────────────────────────

# Groq AI — https://console.groq.com/keys
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# MongoDB Atlas — https://cloud.mongodb.com (free tier M0)
# Formato: mongodb+srv://usuario:contraseña@cluster.mongodb.net/slidehub_ai?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/slidehub_ai

# Render: Deploy Hooks (obtenlos en Settings > Deploy Hook de cada servicio)
# Se usan en el workflow de GitHub Actions para forzar deploy solo si el CI pasa.
# Son OPCIONALES — Render también despliega automáticamente al detectar push a main.
RENDER_DEPLOY_HOOK_GATEWAY=https://api.render.com/deploy/srv-xxxx?key=xxxx
RENDER_DEPLOY_HOOK_CORE=https://api.render.com/deploy/srv-xxxx?key=xxxx
RENDER_DEPLOY_HOOK_AI=https://api.render.com/deploy/srv-xxxx?key=xxxx
